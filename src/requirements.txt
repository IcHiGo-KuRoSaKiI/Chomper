# ============================================================================
# Parsers Module - Requirements
# ============================================================================
# A professional, production-ready document parsing module supporting 9 formats:
# PDF, DOCX, PPTX, Code, Text, Markdown, Excel, CSV, HTML
#
# Installation:
#   pip install -r requirements.txt              # Install all dependencies
#   pip install -r requirements.txt --no-deps    # Core only (manual install)
#
# For minimal installation, see requirements-minimal.txt
# ============================================================================

# ----------------------------------------------------------------------------
# CORE DEPENDENCIES (Always Required)
# ----------------------------------------------------------------------------

# Data handling
dataclasses-json>=0.6.0        # JSON serialization for dataclasses
pydantic>=2.0.0                # Data validation

# Text processing
nltk>=3.8.0                    # Natural language processing
spacy>=3.7.0                   # Advanced NLP (download models: python -m spacy download en_core_web_sm)
python-magic>=0.4.27           # File type detection
chardet>=5.2.0                 # Character encoding detection

# ----------------------------------------------------------------------------
# PDF SUPPORT
# ----------------------------------------------------------------------------

# PDF extraction (choose one or both)
pymupdf>=1.23.0                # Fast PDF extraction (recommended)
# pypdf>=3.17.0                # Alternative PDF library (uncomment if needed)

# PDF image extraction
pillow>=10.1.0                 # Image processing

# ----------------------------------------------------------------------------
# DOCX/PPTX SUPPORT (Microsoft Office)
# ----------------------------------------------------------------------------

python-docx>=1.1.0             # Word document extraction
python-pptx>=0.6.23            # PowerPoint extraction

# ----------------------------------------------------------------------------
# EXCEL/CSV SUPPORT
# ----------------------------------------------------------------------------

openpyxl>=3.1.2                # Excel file parsing (.xlsx, .xlsm)
pandas>=2.1.0                  # CSV parsing and data manipulation
xlrd>=2.0.1                    # Legacy Excel support (.xls) - optional

# ----------------------------------------------------------------------------
# HTML SUPPORT
# ----------------------------------------------------------------------------

beautifulsoup4>=4.12.0         # HTML parsing
lxml>=5.0.0                    # Fast HTML/XML parser
trafilatura>=1.6.0             # Article extraction and boilerplate removal

# ----------------------------------------------------------------------------
# OPTIONAL: Enhanced Features
# ----------------------------------------------------------------------------

# Advanced NLP models (optional, requires manual download)
# spacy models: python -m spacy download en_core_web_sm
# transformers>=4.35.0         # Hugging Face models for advanced NLP

# Image OCR (optional, requires Tesseract installation)
# pytesseract>=0.3.10          # OCR for images in PDFs
# pdf2image>=1.16.3            # Convert PDF pages to images

# Web scraping (optional, for fetching HTML from URLs)
# requests>=2.31.0             # HTTP library
# httpx>=0.25.0                # Async HTTP client

# Database support (optional, for LangFuse/observability)
# psycopg2-binary>=2.9.9       # PostgreSQL adapter

# ----------------------------------------------------------------------------
# DEVELOPMENT DEPENDENCIES (Optional)
# ----------------------------------------------------------------------------

# Testing
# pytest>=7.4.3                # Testing framework
# pytest-cov>=4.1.0            # Coverage reporting

# Code quality
# black>=23.12.0               # Code formatting
# flake8>=6.1.0                # Linting
# mypy>=1.7.0                  # Type checking

# Documentation
# sphinx>=7.2.6                # Documentation generation
# sphinx-rtd-theme>=2.0.0      # ReadTheDocs theme
